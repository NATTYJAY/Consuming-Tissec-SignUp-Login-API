Consuming-Tissec-SignUp-Login-API with LARAVEL Framework

## Installation
```
Clone the repository 

Run `Composer Install` to install dependencies

Start the project by runing `php artisan serve` in your Cmd

Good Luck
```
